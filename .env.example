# Copie para .env e preencha. NUNCA versione o .env real.

# --- LLM (Google Gemini) ---
GEMINI_API_KEY=your-key-here
GEMINI_MODEL=gemini-2.5-flash

# --- Vector DB (RAG - base de cases) ---
VECTOR_DB=chroma
VECTOR_DB_PATH=./vector_store

# --- App ---
APP_ENV=dev
LOG_LEVEL=INFO
